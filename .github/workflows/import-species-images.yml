name: Import reviewed species images

on:
  push:
    branches:
      - feature/species-expansion-04-75
    paths:
      - scripts/species-image-imports-04.json
      - scripts/import-species-images.mjs
      - scripts/apply-species-image-ui-04.mjs
      - scripts/apply-species-release-docs-04.mjs
      - scripts/apply-species-release-cleanups-04.mjs
      - src/lib/engine/species-expansion-04.test.ts
      - .github/workflows/import-species-images.yml

permissions:
  contents: write

jobs:
  import-images:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: Check out feature branch
        uses: actions/checkout@v4
        with:
          ref: feature/species-expansion-04-75
          fetch-depth: 0

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Install dependencies and image processor
        run: npm install --no-save --ignore-scripts=false sharp@0.34.3

      - name: Apply reviewed release cleanups
        run: node scripts/apply-species-release-cleanups-04.mjs

      - name: Download and optimize reviewed canonical images
        run: node scripts/import-species-images.mjs scripts/species-image-imports-04.json

      - name: Verify all 15 canonical and thumbnail assets exist
        shell: bash
        run: |
          set -euo pipefail
          count_canonical=$(find public/species -type f -name canonical.webp | wc -l | tr -d ' ')
          count_thumb=$(find public/species -type f -name thumb.webp | wc -l | tr -d ' ')
          if [ "$count_canonical" -lt 15 ] || [ "$count_thumb" -lt 15 ]; then
            echo "Expected at least 15 canonical and 15 thumbnail files; found canonical=$count_canonical thumb=$count_thumb"
            exit 1
          fi

      - name: Surface reviewed images in species picker
        run: node scripts/apply-species-image-ui-04.mjs

      - name: Update release documentation
        run: node scripts/apply-species-release-docs-04.mjs

      - name: Run engine release-gate tests
        run: node --experimental-strip-types --test src/lib/engine/infer.test.ts src/lib/engine/population-context.test.ts src/lib/engine/species-expansion-04.test.ts

      - name: Typecheck
        run: npm run typecheck

      - name: Build production bundle
        run: npm run build

      - name: Commit repository-owned image assets and UI integration
        shell: bash
        run: |
          set -euo pipefail
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add public/species src/components/instrument.tsx README.md scripts/species-image-imports-04.json src/lib/knowledge/species-images.ts src/lib/engine/infer.test.ts src/lib/engine/species-weight-overrides.ts
          if git diff --cached --quiet; then
            echo "No generated release changes to commit."
            exit 0
          fi
          git commit -m "Import reviewed canonical images for species expansion 04"
          git push origin HEAD:feature/species-expansion-04-75
