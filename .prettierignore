# Generated by @tanstack/router-plugin on every build. Formatting it creates a
# diff the next build reverses.
src/routeTree.gen.ts

# FLEET-SHARED, BYTE-IDENTICAL ACROSS ALL SEVEN HOOK THE HORIZON APPS.
#
# `scripts/sync-fleet-shared.mjs` in field-ops-desk copies these out of the
# canonical checkout and every repo must resolve to the same blob. Prettier
# rewriting one copy is exactly how they drift: it changes whitespace, nothing
# breaks, no test fails, and the fleet is quietly out of step until somebody
# diffs seven repositories by hand. It has already happened twice — to
# `lib/field-mode` and to `lib/fleet-conformance.ts`, both of which promised
# byte-identity in their own headers while being kept in step by hand.
#
# Format these in the canonical repo and re-sync. Never here.
src/lib/field-plates/
src/lib/field-mode/
src/lib/hth-packet.ts
src/lib/fleet-conformance.ts
