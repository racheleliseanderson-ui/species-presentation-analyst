/**
 * Legacy mount retained temporarily so existing root imports remain stable.
 * Support now lives as a native footer link; no third-party widget script is loaded.
 */
export function BuyMeACoffeeWidget() {
  return null;
}
